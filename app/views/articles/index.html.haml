%h1 Listing articles


%table
  %tr
    %th Title
    %th Summary
    %th Url
    %th
    %th
    %th

  - @articles.each do |article|

    %tr
      %td= article.title
      %td= article.summary
      %td= article.url
      %td= link_to 'Show', article
      %td.bookmark= link_to 'bookmark', new_articles_user_path, :data => {:id => article.id}
      %td= link_to 'Destroy', article, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

#paginator
  = paginate @articles

